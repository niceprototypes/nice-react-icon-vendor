[2026-05-23 21:42] major: Replace `lucide-react` runtime dependency with `lucide` (the framework-agnostic data-only package) + an internal `VendorIcon` renderer. Same Lucide icon coverage; the React middleware layer is now owned by this package (~30 LOC in `src/components/VendorIcon/`). Public API unchanged — `<Icon vendor name="…" />` on `nice-react-icon` continues to resolve through `resolveVendorIcon`, which now binds the lucide iconNode tuple into a Bound React component delegating to VendorIcon. Consumers see no API change but get a one-runtime-dep-lighter graph.